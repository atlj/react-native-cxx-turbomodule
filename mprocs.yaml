procs:
  Install pods:
    shell:  pod install
    cwd: example/ios
    autostart: false
  Run Codegen:
    shell: npx react-native codegen
    cwd: example
    autostart: false
  Start example:
    shell: yarn start
    cwd: example
  Build server: xcode-build-server
